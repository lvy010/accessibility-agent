# 安全说明

## 信任边界

本插件在 VS Code 扩展主机中以用户权限运行，需要可信、非虚拟工作区。生产使用前须遵循组织的源码发送、模型服务和站点检测政策。

1. **模型输出不可信**：只接受严格 JSON 方案；不执行命令、不创建删除文件，只在用户授权文件快照上做唯一精确匹配替换。AI 仍可能提出逻辑错误或不安全代码，必须审查差异并测试。
2. **页面不可信**：受限的 Playwright 请求策略仅减小误操作范围，不构成完整网络隔离、SSRF 防火墙或恶意网页沙箱。不允许扫描无授权目标，不加载个人浏览器配置，不读取凭据。
3. **工作区任务有权限**：任务及其依赖可能执行任意代码。仅执行用户明确选择并再次批准的前台任务；超时请求终止并不保证其产生的所有外部进程或远程操作都已停止。
4. **文件并发与保存**：应用前复核内容和磁盘快照，拒绝越界符号链接及已变化文件。保存会触发现有工作区保存钩子。插件不能替代操作系统事务或阻止其他扩展同时修改文件；交付前检查 Git diff。

## 数据处理

- 不实现遥测、自建 API 或密钥存储；使用 VS Code 的用户模型授权。
- 经逐次授权将所选源码发送到模型服务；检测摘要含规则和有限节点选择器，不含完整 DOM、截图、Cookie。模型供应商的数据保留规则由供应商决定。
- 本地报告保留最多 20 份，包含相对文件路径、问题证据和不含查询参数的页面路径；路径本身也可能敏感。原始源码快照、差异和撤销数据只存当前进程内存。
- 输出通道只记录运行 ID 和阶段，不记录提示、原始响应或源码。通用异常消息仍应在分享前审查。
- 可通过 VS Code 的“Developer: Open Extension Logs Folder”定位扩展日志；报告位于 `ExtensionContext.globalStorageUri` 下的 reports 子目录，远程开发时在远程主机。报告与工作区日志均不应公开发布。

## 报告问题

请通过仓库的 GitHub 私有漏洞报告功能报告安全问题（若已启用）；否则先通过维护者公开联系方式请求私下沟通。不要在公开 Issue 中上传密钥、私有源码、站点凭据或未经脱敏的报告。

本项目没有声称通过第三方渗透测试、隐私认证或完整无障碍合规认证。正式推广前应完成组织安全评审、真实账号联调与发布验收。